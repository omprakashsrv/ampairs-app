<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      grant-bluetooth declares minSdk 26, but the app ships minSdk 24. Force the merge so we keep
      Android 7.0/7.1 (API 24-25) support. The Grant Bluetooth path only runs when a user connects a
      Bluetooth printer; on API 24-25 that flow may fail at runtime (acceptable trade-off — those
      devices are a negligible share and Bluetooth printing is optional).
    -->
    <uses-sdk tools:overrideLibrary="dev.brewkits.grant.bluetooth, dev.brewkits.grant" />

    <!-- Network thermal printers (raw TCP :9100) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- FCM push notifications (Android 13+ runtime permission) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Classic Bluetooth (SPP/RFCOMM) thermal printers -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <!-- USB thermal printers (per-device permission granted at runtime via PendingIntent) -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <application
        android:name="com.ampairs.app.MainApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Splash"
        android:usesCleartextTraffic="true">

        <!-- Google Maps API Key — set MAPS_API_KEY in local.properties -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- FCM: handles token refresh + incoming messages -->
        <service
            android:name="com.ampairs.app.AmpairsFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Default channel FCM uses for "notification" messages when the app is backgrounded -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <activity
            android:name="com.ampairs.app.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|mnc|colorMode|density|fontScale|fontWeightAdjustment|keyboard|layoutDirection|locale|mcc|navigation|smallestScreenSize|touchscreen|uiMode"
                android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

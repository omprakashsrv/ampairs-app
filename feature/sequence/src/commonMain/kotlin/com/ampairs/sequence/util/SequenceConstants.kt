package com.ampairs.sequence.util

object SequenceConstants {
    /** Client-side UID prefix for offline-created definitions (server keeps it on upsert). */
    const val SEQUENCE_DEFINITION_UID_PREFIX = "SQD"
    const val MAX_PADDING_LENGTH = 18
}
